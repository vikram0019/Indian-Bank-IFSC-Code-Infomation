{
  "name": "indian-bank-ifsc-finder",
  "version": "1.0.0",
  "private": true,
  "description": "Search engine for Indian bank IFSC codes and branch details",
  "license": "MIT",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "seed": "npm run seed --workspace=server"
  }
}
